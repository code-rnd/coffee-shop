export enum RouteUrls {
  MAIN = "",
  LOGIN = "/login",
  PAY = "/pay",
  PERSONAL = "/personal",
  PRODUCTS = "/products/",
  PROMO_CODE = "/promo-code",
}

export enum ActionUrls {
  CREATE = "create",
  VIEW = "view",
  EDIT = "edit",
}
